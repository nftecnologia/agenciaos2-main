# Exemplo de variáveis para Railway
# Copie este arquivo e ajuste os valores

# Database (será fornecido pelo Railway se usar Railway Postgres)
DATABASE_URL=postgresql://username:password@host:port/database

# Redis (será criado automaticamente pelo Railway)
REDIS_URL=redis://default:password@host:port

# NextAuth (gere uma chave secreta)
NEXTAUTH_SECRET=sua-chave-secreta-aqui
NEXTAUTH_URL=https://seu-app.railway.app

# OpenAI
OPENAI_API_KEY=sk-sua-chave-openai-aqui

# Node Environment
NODE_ENV=production