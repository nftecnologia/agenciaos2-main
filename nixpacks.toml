[variables]
NODE_ENV = "production"
SKIP_ENV_VALIDATION = "1"

[phases.setup]
nixPkgs = ["nodejs_20", "npm-9_x", "chromium"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"